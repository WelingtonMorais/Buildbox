import React from 'react';
import { HeaderWrapper, HeaderContent, Title, Subtitle } from './styles';


const Header = () => {
  return (
    <HeaderWrapper>
      <HeaderContent>
        <Title>Buildbox</Title>
        <Subtitle>WEB CHALLENGE</Subtitle>
      </HeaderContent>
    </HeaderWrapper>
  );
};

export default Header;
